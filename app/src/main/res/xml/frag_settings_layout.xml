<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/frag_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <SwitchPreference android:title="Automatically Upload"
        android:summary="Automatically Upload Photo when Task is Created"
        android:key="autoupload"/>
    <SwitchPreference android:title="Wifi Upload Only"
        android:summary="Only Allow Upload when Connected to Wifi"
        android:key="wifiupload"/>
</PreferenceScreen>
